<h1>Showing {{ $page->name }}</h1>

	<div class="jumbotron text-center">
		<h2>{{ $page->name }}</h2>
		<p>
			<strong>Name:</strong> {{ $page->name }}<br>
			<strong>RTE:</strong> {{ $page->richtext }}
		</p>
	</div>